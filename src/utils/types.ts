export type BlogCardProps = {
    title: string
    description: string
    imgUrl: string
    duration: string
}

export type AboutHoveredStackBoxProps = {
    title: string,
    description: string,
    imgUrl: string
}